
// To find the GCD in the strings 
// e.g -> Input: (str1 = "ABCABC" -> 2*"ABC", str2 = "ABC") --> Output: "ABC"   
//     -> Input: (str1 = "ABABAB" -> 3*"AB", str2 = "ABAB" -> 2*"AB") --> Output: "AB"
//     -> Input: (str1 = "LEET", str2 = "CODE")    --> Output: ""

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// char * gcdOfStrings(char *str1, char *str2){
//     int min = (strlen(str1) < strlen(str2)) ? strlen(str1) : strlen(str2);
//     char gcd[min+1];

//     int i=0;
//     while(str1[0]!='\0'){
        
//     }
// }

int main(){
    // int str1, str2;
    // printf("Enter the first string: ");
    // scanf("%s", &str1);

    // printf("Enter the second string: ");
    // scanf("%s", &str2);

    // gcdOfStrings(str1, str2);

    int a[] = {1,2,3,4,5};
}