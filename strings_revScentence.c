// incomplete
sdfs
#include <stdio.h>
#include <string.h>

char *reverseWords(char *s){
    int len = strlen(s);
    char str[len];
    while(s[0]!='\0'){
        
    }
}

int main(){
    char str[50];
    printf("Enter the scentence you want to reverse: ");
    scanf("%[^\n]", &str);
    char *s = &str;

    reverseWords(s);
}